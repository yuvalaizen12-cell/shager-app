"use client";
export const dynamic = "force-dynamic";

export default function OrdersPage() {
  return (
    <main dir="rtl" style={{ padding: 16 }}>
      בדיקת Orders — עמוד מינימלי (ללא DB)
    </main>
  );
}
